version: 0.2

phases:
  install:
    commands:
      - echo Installing frontend dependencies...
      - npm install
  build:
    commands:
      - echo Building the React app...
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build
